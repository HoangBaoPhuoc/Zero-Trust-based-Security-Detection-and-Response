version: '3.8'
services:
  api-gateway:
    build:
      context: ..
      dockerfile: services/Dockerfile
      args:
        SERVICE_NAME: api-gateway
    ports: ["8080:8080"]
  payment-service:
    build:
      context: ..
      dockerfile: services/Dockerfile
      args:
        SERVICE_NAME: payment-service
# TODO: complete local stack from IMPLEMENTATION.md §6.11
