apiVersion: apps/v1
kind: Deployment
metadata:
  name: loki
  namespace: plg-stack
spec:
  replicas: 1
  selector:
    matchLabels:
      app: loki
  template:
    metadata:
      labels:
        app: loki
    spec:
      containers:
        - name: loki
          image: grafana/loki:3.1.0
          args: ["-config.file=/etc/loki/loki-config.yml"]
          ports:
            - containerPort: 3100
---
apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: plg-stack
spec:
  selector:
    app: loki
  ports:
    - port: 3100
      targetPort: 3100
