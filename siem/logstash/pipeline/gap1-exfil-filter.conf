# Gap 1 fix: large response exfiltration detection
if [log_type] == "envoy_access" and [cloud_provider] == "openstack" {
  if [bytes_sent] > 1048576 {
    mutate { add_field => { "alert_type" => "large_response_exfil_suspect" } }
  }
}
